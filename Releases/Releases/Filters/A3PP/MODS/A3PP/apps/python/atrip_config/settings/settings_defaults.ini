[GENERAL]
save_load_ini = False ; Save and load custom values from ini (Every value change in sol_config.lua will be saved in sol_config.ini in apps folder. If value in lua is different from ini ie. update, values from ini will be restored and loaded into lua before launch of the game) ; "True" or "False"